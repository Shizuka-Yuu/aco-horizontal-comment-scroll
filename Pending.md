DOMモード、APIモード時にそれぞれ別にフォントサイズ・スクロール速度・レーン数設定などを設定可能とする
これはスクリーンサイズが変化する事によりコメントの視認性もシームレスにユーザーの希望に合わせるため

システムメッセージの流し込みタイミングの調整
即時放流をしてしまうと、右からコメントエリアがせり出したタイミング時にスクリーン領域が変化する為、コメントの衝突が発生してしまう
これはシステムメッセージだけの問題ではなく通常メッセージでも同様の衝突が起こる可能性がある
モード切替時に2~3秒程度待ち時間を挟み、そこからコメントが流れるようにすれば、システムメッセージの最優先放流ルールは変えずに、強制的なコメント衝突は避けられるのではないか？


コメントの洪水対策の30秒ルールがAPIモードからDOMモードに切り替えた際に毎回発火しており、この時にコメントが重複してしまっている
ユーザーが任意でコメントエリアを展開した場合は30秒ルールとは別にAPIモード中に最後に流れたコメントとコメントエリアを展開しDOMが生成された中からAPIモード中最後のコメントとを一致させ、以降のコメントから流すようにすれば重複は避けられるし
API→DOMの切り替わりが完全にシームレスになる？
現状の仕様ではDOM→APIは自然